app.controller("defaultController", ['$scope', '$rootScope', '$http', 'ModalService', 'Notification', function ($scope, $rootScope, $http, ModalService, Notification) {

    //inicializarTabs();
    //$rootScope.cantidadRegistros = 0;
    //function inicializarTabs() {
    //    FacturaService.obtenerTotalFacturasPorFirmar()
    //    .then(function (result) {
    //        $rootScope.cantidadRegistros = result.data;
    //    }, function (reason) {
    //        msg = { title: 'Error obteniendo cantidad de facturas por firmar' };
    //        Notification.error(msg);
    //        $element.modal('hide');
    //        close(true, 500);
    //    });
    //}

}]);